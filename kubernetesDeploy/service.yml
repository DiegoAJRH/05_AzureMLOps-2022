apiVersion: v1
kind: Service
metadata:
  name: animals-classification
  labels:
    app: animals-classification
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: animals-classification
    nodePort: 30080 
  selector:
    app: animals-classification
